import 'package:flutter/material.dart';

class TripDetailsStep extends StatelessWidget {
  const TripDetailsStep({required this.child, super.key});
  final Widget child;

  @override
  Widget build(BuildContext context) => child;
}
