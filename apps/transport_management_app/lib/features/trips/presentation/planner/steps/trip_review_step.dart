import 'package:flutter/material.dart';

class TripReviewStep extends StatelessWidget {
  const TripReviewStep({required this.child, super.key});
  final Widget child;

  @override
  Widget build(BuildContext context) => child;
}
