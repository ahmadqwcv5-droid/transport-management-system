import 'package:flutter/material.dart';

class TripAssignmentStep extends StatelessWidget {
  const TripAssignmentStep({required this.child, super.key});
  final Widget child;

  @override
  Widget build(BuildContext context) => child;
}
